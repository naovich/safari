import { Dancing_Script } from "next/font/google";
import { Pacifico } from "next/font/google";
import { Patua_One } from "next/font/google";

export const patuaFont = Patua_One({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
export const pacificoFont = Pacifico({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const dancingFont = Dancing_Script({
  weight: "700",
  subsets: ["latin"],
  display: "swap",
});
